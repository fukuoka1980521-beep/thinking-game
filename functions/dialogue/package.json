{
  "name": "thinking-game-dialogue-function",
  "version": "1.0.0",
  "private": true,
  "description": "PERSONALIZED_DIALOGUE_AND_VISUAL_EXPERIENCE Run / real AI dialogue Run: minimal stateless HTTP Cloud Function that proxies a single CASE-001 dialogue turn to Vertex AI Gemini. Holds no secret material -- authenticates to Vertex AI via the function's own Cloud Run service account (Application Default Credentials), never a hardcoded API key. Never receives rubric ground truth. Never persists anything.",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "@google/genai": "^0.3.0"
  }
}
